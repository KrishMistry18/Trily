export enum JobType {
  CREATE = "CREATE",
  EDIT = "EDIT",
}

export enum JobStatus {
  PENDING = "PENDING",
  PROCESSING = "PROCESSING",
  COMPLETED = "COMPLETED",
  FAILED = "FAILED",
}

export enum Tier {
  FREE = "FREE",
  PRO = "PRO",
  BUSINESS = "BUSINESS",
}

export enum ChatStatus {
  PENDING = "PENDING",
  APPLIED = "APPLIED",
  FAILED = "FAILED",
}
